#!/usr/bin/env ruby
# bin/debugr
require_relative '../lib/debugr/session'

if ARGV.empty?
  puts 'Usage: debugr <script.rb> [args...]'
  exit 1
end


script = ARGV.shift
session = Debugr::Session.new(script, ARGV)
session.run