#!/usr/bin/env ruby

require_relative '../lib/debugr/session'

if ARGV.empty?
  puts 'Usage: debugga <script.rb> [args...]'
  exit 1
end

script = ARGV.shift
session = Debugr::Session.new(script, ARGV)
session.engine.step!
session.run
